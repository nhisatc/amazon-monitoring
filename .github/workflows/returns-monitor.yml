name: Return Reason Monitor

permissions:
  contents: write

on:
  schedule:
    # Amazon's returns report lags the physical return by a few hours, so
    # polling faster than this buys nothing. 06:00, 12:00, 18:00, 00:00 UTC.
    - cron: '0 0,6,12,18 * * *'
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Preview only — render the alert but send nothing and save no state'
        type: boolean
        default: false

concurrency:
  group: returns-monitor
  cancel-in-progress: false

jobs:
  returns-monitor:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests python-dotenv

      - name: Run returns monitor
        env:
          LWA_APP_ID:           ${{ secrets.LWA_APP_ID }}
          LWA_CLIENT_SECRET:    ${{ secrets.LWA_CLIENT_SECRET }}
          SP_API_REFRESH_TOKEN: ${{ secrets.SP_API_REFRESH_TOKEN }}
          SELLER_ID:            ${{ secrets.SELLER_ID }}
          MARKETPLACE_ID:       ${{ secrets.MARKETPLACE_ID }}
          GMAIL_ADDRESS:        ${{ secrets.GMAIL_ADDRESS }}
          GMAIL_APP_PASSWORD:   ${{ secrets.GMAIL_APP_PASSWORD }}
          SMTP_FROM_ADDRESS:    ${{ secrets.SMTP_FROM_ADDRESS }}
          SMTP_FROM_PASSWORD:   ${{ secrets.SMTP_FROM_PASSWORD }}
          ALERT_RECIPIENTS:     ${{ secrets.ALERT_RECIPIENTS }}
          SLACK_BOT_TOKEN:      ${{ secrets.SLACK_BOT_TOKEN }}
          SLACK_CHANNEL_ID:     ${{ secrets.SLACK_CHANNEL_ID }}
        run: python returns_monitor.py ${{ inputs.dry_run && '--dry-run' || '' }}

      - name: Commit state
        if: ${{ !inputs.dry_run }}
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add data/
          git diff --staged --quiet && echo "No changes" && exit 0
          git commit -m "Auto: returns monitor state updated"
          for i in 1 2 3; do
            git pull --rebase --autostash origin main && git push && exit 0
            echo "Push race, retry $i"
            sleep 5
          done
          echo "Could not push state after 3 attempts" >&2
          exit 1
